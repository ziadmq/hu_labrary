package com.hu.library.ui.theme

import androidx.compose.ui.graphics.Color

// === HU Identity Palette (Red & Gold) ===

// Primary: HU Deep Red (Royal & Academic)
val PrimaryColor = Color(0xFFB71C1C)       // Deep Crimson Red
val PrimaryVariant = Color(0xFF7F0000)     // Dark Maroon (for gradients)

// Secondary: University Gold (Perfect contrast with Red)
val SecondaryColor = Color(0xFFFFC107)     // Amber / Gold
val SecondaryContainer = Color(0xFFFFF8E1) // Light Gold background

// Accents
val ActionColor = Color(0xFFB71C1C)        // Use Red for "See All" buttons
val CreativeOrange = Color(0xFFFF5722)     // Bright Orange for highlights

// Backgrounds (Clean White/Grey)
val BackgroundColor = Color(0xFFFAFAFA)    // Very light grey (almost white)
val SurfaceColor = Color(0xFFFFFFFF)       // Pure White

// Text
val TextPrimary = Color(0xFF212121)        // Soft Black
val TextSecondary = Color(0xFF757575)      // Grey

// Status Indicators
val SuccessGreen = Color(0xFF388E3C)       // Green
val ErrorRed = Color(0xFFD32F2F)           // Red
val WarningYellow = Color(0xFFFBC02D)      // Yellow